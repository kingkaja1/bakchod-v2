rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Chat media: only authenticated users can read/write
    match /chatMedia/{chatId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
